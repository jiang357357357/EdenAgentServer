# Connector compatibility manifests

This directory is intentionally kept in source control as the optional legacy
manifest overlay used by `mon-agent-connectors`.

New connectors should normally be shipped as packages under `Connectors/`.
Only JSON files are loaded from this directory, so this README has no effect on
the runtime catalog.
