import type { ToolProvider } from "../types"

export function loadMcpToolProviders(): ToolProvider[] {
  // MCP tool bridging will be wired here after MonAgent has a stable MCP server config format.
  return []
}
